# 단기/장기 매매신호 (이격도 기반) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 검색한 티커의 현재가와 50/200일 이동평균으로 이격도를 계산해 7단계 단기/장기 매매신호를 카드 "추세" 블록에 표시한다.

**Architecture:** 순수 계산(이격도 + 신호 레벨)은 `indicators.ts`에 두고 Vitest로 검증한다. 레벨 키만 `EnrichedStock`에 싣고(`enrich.ts`), 라벨·이모지·색상 매핑은 `StockAnalysis.tsx`가 소유한다. 새 API·외부 호출 없음.

**Tech Stack:** Next.js 15 (App Router) · TypeScript · Tailwind · Vitest · path alias `@/` → `src/`

## Global Constraints

- 외부 호출은 전부 서버 라우트(`/api/*`)에서만. 이 기능은 외부 호출을 추가하지 않는다(기존 데이터 재사용).
- 테스트는 `test/` 디렉터리에 위치(코로케이션 아님). 실행: `npm test` (= `vitest run`).
- 이격도 정의: `(price − sma) / sma × 100`.
- 신호 임계값(0 기준 대칭, 절댓값 하한 포함): `|d| < 5` 정상 · `5 ≤ |d| < 10` 약 · `10 ≤ |d| < 15` 중 · `|d| ≥ 15` 강.
- 데이터 부족(sma null) 또는 price null 또는 sma 0 → 이격도/신호 null → UI "—" 또는 줄 생략.
- 커밋 규칙: 사용자 글로벌 RULES.md = "명시 요청 없이 자동 커밋 금지". 아래 커밋 스텝은 실행 단계에서 사용자가 진행을 승인할 때만 수행한다.

---

### Task 1: 이격도 + 신호 레벨 순수 함수

**Files:**
- Modify: `src/lib/types.ts` (add `MaSignalLevel` type)
- Modify: `src/lib/indicators.ts` (add `disparity`, `maSignal`)
- Test: `test/indicators.test.ts` (append `disparity`, `maSignal` describes)

**Interfaces:**
- Consumes: 없음 (순수 함수)
- Produces:
  - `type MaSignalLevel = "strong-overheated" | "overheated" | "weak-overheated" | "normal" | "weak-oversold" | "oversold" | "strong-oversold"` (in `types.ts`)
  - `disparity(price: number | null, sma: number | null): number | null`
  - `maSignal(disparityPct: number | null): MaSignalLevel | null`

- [ ] **Step 1: Add `MaSignalLevel` to `src/lib/types.ts`**

`CrossState`(현재 27번째 줄 부근) 바로 아래에 추가:

```ts
export type MaSignalLevel =
  | "strong-overheated"
  | "overheated"
  | "weak-overheated"
  | "normal"
  | "weak-oversold"
  | "oversold"
  | "strong-oversold";
```

- [ ] **Step 2: Write the failing tests in `test/indicators.test.ts`**

Import 라인에 `disparity, maSignal`를 추가하고(현재: `import { sma, rsi14, crossState, crossFreshDays, range52w, volumeSpike } from "@/lib/indicators";`), 파일 끝에 추가:

```ts
describe("disparity", () => {
  it("is positive when price is above the moving average", () => {
    expect(disparity(110, 100)).toBeCloseTo(10, 6);
  });
  it("is negative when price is below the moving average", () => {
    expect(disparity(95, 100)).toBeCloseTo(-5, 6);
  });
  it("is null when sma is null, zero, or price is null", () => {
    expect(disparity(100, null)).toBeNull();
    expect(disparity(100, 0)).toBeNull();
    expect(disparity(null, 100)).toBeNull();
  });
});

describe("maSignal", () => {
  it("returns null for null input", () => {
    expect(maSignal(null)).toBeNull();
  });
  it("is normal inside ±5%", () => {
    expect(maSignal(0)).toBe("normal");
    expect(maSignal(4.9)).toBe("normal");
    expect(maSignal(-4.9)).toBe("normal");
  });
  it("is weak between 5 and 10 (boundary 5 inclusive)", () => {
    expect(maSignal(5)).toBe("weak-overheated");
    expect(maSignal(9.9)).toBe("weak-overheated");
    expect(maSignal(-5)).toBe("weak-oversold");
    expect(maSignal(-9.9)).toBe("weak-oversold");
  });
  it("is mid between 10 and 15 (boundary 10 inclusive)", () => {
    expect(maSignal(10)).toBe("overheated");
    expect(maSignal(14.9)).toBe("overheated");
    expect(maSignal(-10)).toBe("oversold");
    expect(maSignal(-14.9)).toBe("oversold");
  });
  it("is strong at 15 and beyond (boundary 15 inclusive)", () => {
    expect(maSignal(15)).toBe("strong-overheated");
    expect(maSignal(30)).toBe("strong-overheated");
    expect(maSignal(-15)).toBe("strong-oversold");
    expect(maSignal(-30)).toBe("strong-oversold");
  });
  it("matches sheet samples", () => {
    expect(maSignal(-0.5)).toBe("normal");
    expect(maSignal(-7.2)).toBe("weak-oversold");
    expect(maSignal(-19.66)).toBe("strong-oversold");
    expect(maSignal(20.21)).toBe("strong-overheated");
  });
});
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `npm test -- indicators`
Expected: FAIL — `disparity is not a function` / `maSignal is not a function`.

- [ ] **Step 4: Implement in `src/lib/indicators.ts`**

상단 import을 `CrossState`와 함께 `MaSignalLevel`을 가져오도록 수정:

```ts
import type { CrossState, MaSignalLevel } from "@/lib/types";
```

파일 끝에 추가:

```ts
/** 이격도(%) = (price − sma) / sma × 100. price/sma가 null이거나 sma가 0이면 null. */
export function disparity(price: number | null, sma: number | null): number | null {
  if (price == null || sma == null || sma === 0) return null;
  return ((price - sma) / sma) * 100;
}

/** 이격도(%)를 7단계 매매신호 레벨로 매핑. null이면 null. 임계값 ±5/±10/±15(하한 포함). */
export function maSignal(disparityPct: number | null): MaSignalLevel | null {
  if (disparityPct == null) return null;
  const a = Math.abs(disparityPct);
  if (a < 5) return "normal";
  const over = disparityPct > 0;
  if (a < 10) return over ? "weak-overheated" : "weak-oversold";
  if (a < 15) return over ? "overheated" : "oversold";
  return over ? "strong-overheated" : "strong-oversold";
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npm test -- indicators`
Expected: PASS (기존 + 신규 describe 전부 통과).

- [ ] **Step 6: Commit** (실행 단계에서 사용자 승인 시)

```bash
git add src/lib/types.ts src/lib/indicators.ts test/indicators.test.ts
git commit -m "feat: disparity + 7-tier MA signal logic (이격도 매매신호)"
```

---

### Task 2: EnrichedStock에 이격도/신호 필드 연결

**Files:**
- Modify: `src/lib/types.ts` (add 4 fields to `EnrichedStock`)
- Modify: `src/lib/enrich.ts` (compute + populate)
- Test: `test/enrich.test.ts` (append assertions)

**Interfaces:**
- Consumes: `disparity`, `maSignal` (Task 1), `MaSignalLevel` (Task 1), 기존 `sma`
- Produces: `EnrichedStock` 4개 신규 필드 — `disparity50: number | null`, `disparity200: number | null`, `signal50: MaSignalLevel | null`, `signal200: MaSignalLevel | null`

- [ ] **Step 1: Add fields to `EnrichedStock` in `src/lib/types.ts`**

`EnrichedStock`의 `crossState`/`crossFreshDays` 줄 바로 아래에 추가:

```ts
  disparity50: number | null; disparity200: number | null;
  signal50: MaSignalLevel | null; signal200: MaSignalLevel | null;
```

- [ ] **Step 2: Write failing assertions in `test/enrich.test.ts`**

첫 번째 테스트("combines config + market data into an EnrichedStock") 끝의 `expect` 블록에 추가. 해당 테스트는 `closes = 100 - i*0.06` (250개, 우하향)이고 `price = 85`라 50일/200일 이평선 모두 85보다 높음 → 이격도 음수.

```ts
  expect(s.disparity50).not.toBeNull();
  expect(s.disparity50!).toBeLessThan(0);
  expect(s.disparity200).not.toBeNull();
  expect(s.disparity200!).toBeLessThan(0);
  expect(s.signal50).not.toBeNull();
```

두 번째 테스트("uses sheet high override when provided", `history: null`)에 추가 — 이력 없음 → sma null → 이격도/신호 null:

```ts
  expect(s.disparity50).toBeNull();
  expect(s.signal50).toBeNull();
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `npm test -- enrich`
Expected: FAIL — `s.disparity50` 등이 `undefined` (필드 미존재).

- [ ] **Step 4: Implement in `src/lib/enrich.ts`**

import에 `disparity, maSignal` 추가(현재: `import { rsi14, sma, crossState, crossFreshDays, range52w, volumeSpike } from "@/lib/indicators";`):

```ts
import { rsi14, sma, crossState, crossFreshDays, range52w, volumeSpike, disparity, maSignal } from "@/lib/indicators";
```

`const sma50 = sma(closes, 50);` / `const sma200 = sma(closes, 200);` 아래에 추가:

```ts
  const disparity50 = disparity(price, sma50);
  const disparity200 = disparity(price, sma200);
```

`return { ... }` 객체에서 `crossState`/`crossFreshDays` 항목 바로 뒤에 추가:

```ts
    disparity50, disparity200,
    signal50: maSignal(disparity50), signal200: maSignal(disparity200),
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npm test -- enrich`
Expected: PASS.

- [ ] **Step 6: Run full suite + typecheck**

Run: `npm test && npx tsc --noEmit`
Expected: 전체 PASS, 타입 에러 없음.

- [ ] **Step 7: Commit** (실행 단계에서 사용자 승인 시)

```bash
git add src/lib/types.ts src/lib/enrich.ts test/enrich.test.ts
git commit -m "feat: wire disparity/MA signals into EnrichedStock"
```

---

### Task 3: "추세" 블록에 단기/장기 신호 표시

**Files:**
- Modify: `src/components/StockAnalysis.tsx`

**Interfaces:**
- Consumes: `EnrichedStock` 필드 `sma50/sma200/disparity50/disparity200/signal50/signal200`, `MaSignalLevel` 타입
- Produces: 없음 (최종 UI)

- [ ] **Step 1: Add `MaSignalLevel` to the type import**

현재 1번째 줄(`import type { EnrichedStock } from "@/lib/types";`)을 다음으로 교체:

```tsx
import type { EnrichedStock, MaSignalLevel } from "@/lib/types";
```

- [ ] **Step 2: Add the label/emoji/color map and a row component**

`zoneChip` 함수 위(import들 아래)에 추가:

```tsx
const SIGNAL_DISPLAY: Record<MaSignalLevel, { emoji: string; label: string; className: string }> = {
  "strong-overheated": { emoji: "🔴", label: "강과열 · 매도 고려", className: "text-red-500" },
  "overheated": { emoji: "🟠", label: "과열", className: "text-orange-500" },
  "weak-overheated": { emoji: "🟡", label: "약과열", className: "text-amber-500" },
  "normal": { emoji: "🟢", label: "정상", className: "text-green-600" },
  "weak-oversold": { emoji: "🟡", label: "약과매도", className: "text-amber-500" },
  "oversold": { emoji: "🟠", label: "과매도", className: "text-orange-500" },
  "strong-oversold": { emoji: "🔴", label: "강과매도 · 매수 고려", className: "text-red-500" },
};

function SignalRow({
  label, sma, disparity, signal,
}: {
  label: string; sma: number | null; disparity: number | null; signal: MaSignalLevel | null;
}) {
  if (sma == null) return null;
  const sig = signal ? SIGNAL_DISPLAY[signal] : null;
  return (
    <div className="flex flex-wrap items-center gap-x-1.5 text-zinc-500">
      <span>{label}</span>
      <span className="tabular-nums">{sma.toFixed(2)}</span>
      {disparity != null && (
        <span className="tabular-nums">
          {disparity >= 0 ? "+" : ""}
          {disparity.toFixed(2)}%
        </span>
      )}
      {sig && <span className={`font-medium ${sig.className}`}>{sig.emoji} {sig.label}</span>}
    </div>
  );
}
```

- [ ] **Step 3: Replace the static sma line in the "추세" block**

현재 "추세" div 안의 이 줄을 교체:

```tsx
          {stock.sma50 != null && stock.sma200 != null && (
            <div className="text-zinc-500">50일 {stock.sma50.toFixed(1)} / 200일 {stock.sma200.toFixed(1)}</div>
          )}
```

교체 후 (골든/데드크로스 라인은 그대로 두고, 그 아래에 신호 두 줄을 둔다 — 즉 `crossState` div 위치/내용 유지, 위 sma 라인만 아래 두 줄로 대체):

```tsx
          <SignalRow label="단기(50일)" sma={stock.sma50} disparity={stock.disparity50} signal={stock.signal50} />
          <SignalRow label="장기(200일)" sma={stock.sma200} disparity={stock.disparity200} signal={stock.signal200} />
```

결과 "추세" 블록 순서: ① 골든/데드크로스 라인(기존) → ② 단기(50일) 신호 → ③ 장기(200일) 신호 → ④ 52주 위치(기존) → ⑤ 거래량(기존).

- [ ] **Step 4: Typecheck + lint + build**

Run: `npx tsc --noEmit && npm run lint && npm run build`
Expected: 타입 에러 없음, lint 통과, 빌드 성공.

- [ ] **Step 5: Visual verification (manual)**

Run: `npm run dev` 후 종목 검색(예: PLTR — 200일 신호가 🔴 강과매도·매수 고려로 나오는지, AAPL — 200일 🟠 과열 등) 확인. 이력이 짧은 종목(예: SHLD)은 신호 줄이 숨겨지는지 확인.

- [ ] **Step 6: Commit** (실행 단계에서 사용자 승인 시)

```bash
git add src/components/StockAnalysis.tsx
git commit -m "feat: show 단기/장기 매매신호 in 추세 block"
```

---

## Self-Review

**1. Spec coverage:**
- 이격도 계산 → Task 1 (`disparity`). ✅
- 7단계 신호(±5/±10/±15 대칭, 하한 포함) → Task 1 (`maSignal`) + 경계 테스트. ✅
- `EnrichedStock` 4필드 → Task 2. ✅
- 실시간 per-ticker(시트 고정값 아님) → Task 2가 기존 `price`/`sma`를 재사용(검색 티커 일봉). ✅
- "추세" 블록 강화 + 라벨/이모지/색 매핑 분리 → Task 3. ✅
- 데이터 부족/널 처리 → Task 1(null 반환) + Task 2(null 검증) + Task 3(`SignalRow` sma null이면 줄 숨김). ✅
- 새 API·외부호출 없음 → 어느 태스크도 provider/route 변경 안 함. ✅
- 테스트 → Task 1(indicators), Task 2(enrich). ✅

**2. Placeholder scan:** TBD/TODO/"적절히 처리" 없음. 모든 코드 스텝에 실제 코드 포함. ✅

**3. Type consistency:** `MaSignalLevel` 7개 키가 types.ts 정의 → indicators.ts `maSignal` 반환값 → enrich.ts 필드 → StockAnalysis.tsx `SIGNAL_DISPLAY` Record 키까지 동일. `disparity`/`maSignal` 시그니처가 Task 1 정의와 Task 2 호출에서 일치. ✅
